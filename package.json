{
  "name": "scriptflow-pro",
  "version": "2.1.0",
  "private": true,
  "description": "ScriptFlow Pro - Smart CRM with TypeScript source and compiled browser runtime",
  "main": "server.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "node server.js",
    "dev": "npm run build && node server.js"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
